# write a python program to count occurance of each word in a given sentence.
def convert_list(list1):
    return (list1[0].split())
    print(len(list1))
list1 = ["This is about core python"]
print(len(convert_list(list1)))

