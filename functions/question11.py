# write a python program to create a lambda function that adds 15 to a given number passed in as
# a argument, also create a lambda function that multiples argumnet x with argumnet y and
# print the result.  

add = lambda a : a+15
mul = lambda x,y : x * y
print(add(5))
print(mul(5,2))

